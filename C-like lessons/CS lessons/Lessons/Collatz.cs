using System;
using System.Collections.Generic;
using System.Linq;
using System.IO;

namespace Lessons
{
    class Collatz
    {
        //public static void Main()
        //{
        //    string Path = "Resources.txt";
        //    string[] RawInput = File.ReadAllLines(Path);

        //    int[] Numbers = RawInput[1].Split().Select(number => Convert.ToInt32(number)).ToArray();

        //    int[] Results = new int[Numbers.Length];

        //    for (int i = 0; i < Numbers.Length; ++i)
        //    {
        //        while (Numbers[i] != 1)
        //        {
        //            if (Numbers[i] % 2 == 0) Numbers[i] = Numbers[i] = Numbers[i] / 2;
        //            else Numbers[i] = Numbers[i] * 3 + 1;
        //            ++Results[i];
        //        }
        //    }

        //    foreach (var result in Results)
        //    {
        //        Console.Write(result + " ");
        //    }
        //}
    }
}
